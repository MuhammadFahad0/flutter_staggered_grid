# flutter_staggered_grid example

Demo app for the layouts provided by `flutter_staggered_grid`.

Run it with:

```sh
flutter run
```

The app includes pages for staggered, masonry, quilted, woven, staired, and
aligned layouts.
